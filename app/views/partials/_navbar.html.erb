<nav class="bg-gray-800 shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <!-- Logo / Title -->
      <h1 class="text-white text-2xl font-bold tracking-wide">
        Leisontravels
      </h1>

      <!-- Language Switcher -->
      <div class="flex items-center">
        <ul class="flex items-center space-x-6">
          <li>
            <div class="language-switcher text-gray-300">
              <%= link_to "🇬🇧 English", url_for(locale: :en), class: "#{params[:locale] == 'en' ? 'text-white font-bold' : 'hover:text-white'}" %> |
              <%= link_to "🇨🇴 Español", url_for(locale: :es), class: "#{params[:locale] == 'es' ? 'text-white font-bold' : 'hover:text-white'}" %> |
              <%= link_to "🇧🇷 Português", url_for(locale: :pt), class: "#{params[:locale] == 'pt' ? 'text-white font-bold' : 'hover:text-white'}" %>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
